project: predictive_maintenance

registry:
  path: s3://predictive-maintenance-mlops-artifacts-040175285118/registry.pb

provider: aws

offline_store:
  type: file   # sin "path"

# Sin online_store: no hay streaming (core_ml/streaming/ fue eliminado) ni
# nada que materialice o sirva features en tiempo real -- ver
# features.py::engine_window_fv (online=False). Solo se usa
# get_historical_features (Offline Store) para entrenamiento.
